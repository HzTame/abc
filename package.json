{
  "name": "the-audio-vault",
  "version": "1.0.0",
  "private": true,
  "description": "The Audio Vault static site with a protected Node security gateway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check admin.js && node --check upload.js && node --check security.js"
  },
  "engines": {
    "node": ">=20"
  }
}
